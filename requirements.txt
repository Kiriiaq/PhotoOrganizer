# PhotoOrganizer v2.0.0 — runtime dependencies
# Source of truth: pyproject.toml. This file is the synchronised mirror.
# Python 3.11+
#
# Install: pip install -r requirements.txt

customtkinter>=5.2.0,<6.0.0
darkdetect>=0.8.0,<1.0.0
Pillow>=10.0.0,<12.0.0
exifread>=3.0.0,<4.0.0
pillow-heif>=0.13.0,<1.0.0
requests>=2.31.0,<3.0.0
PyYAML>=6.0,<7.0

# Optional extras (install via `pip install -e .[dev,dnd,toast]`)
# tkinterdnd2>=0.4.0   # drag-and-drop
# plyer>=2.1           # Windows toast notifications
