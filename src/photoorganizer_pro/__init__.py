"""PhotoOrganizer Pro — espace réservé.

Ce package est intentionnellement vide. Il marque la frontière entre :

* ``src/`` (cœur ouvert sous Apache-2.0) — fonctionnalités gratuites de la
  GUI PhotoOrganizer ;
* ``src/photoorganizer_pro/`` (réservé) — futures fonctionnalités de la
  version Pro (batch CLI d'organisation, scheduler watch-folder, plugins,
  support officiel).

La séparation est ici structurelle uniquement ; aucun code Pro n'est
encore livré. Voir ``docs/MONETIZATION.md`` (Phase 5) pour la stratégie.
"""
