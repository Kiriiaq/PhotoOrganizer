# PhotoOrganizer v2.0.0 - Dépendances

# Interface graphique moderne
customtkinter>=5.2.0
darkdetect>=0.8.0

# Traitement d'images
Pillow>=10.0.0
exifread>=3.0.0
piexif>=1.1.3

# Support HEIC/HEIF (optionnel mais recommandé)
pillow-heif>=0.13.0

# Support RAW (optionnel)
# rawpy>=0.18.0

# Requêtes HTTP (géocodage)
requests>=2.31.0

# Configuration YAML
PyYAML>=6.0.0

# Corbeille cross-platform (pour mode --trash)
send2trash>=1.8.0

# Barre de progression CLI
tqdm>=4.65.0

# Hash BLAKE3 rapide (optionnel mais recommandé)
blake3>=0.3.0

# Développement (optionnel)
# pytest>=7.0.0
# pytest-cov>=4.0.0
# black>=23.0.0
# flake8>=6.0.0
